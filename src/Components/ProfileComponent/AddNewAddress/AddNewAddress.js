import React from 'react';
import ProfileHeader from '../ProfileHeader/ProfileHeader';
import './AddNewAddress.css';

const AddNewAddress = () => {
    return (
        <div>
            <ProfileHeader>Add new address</ProfileHeader>
        </div>
    );
};

export default AddNewAddress;